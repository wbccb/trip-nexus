# v0.0.6

## 一、版本定位

v0.0.6 的核心目标：在延续 v0.0.5「单主流程」稳定性的前提下，完成从“行程生成工具”到“中立旅行决策与共创平台”的产品能力升级。

本版本不与 OTA 在交易履约上正面竞争，聚焦三类差异化价值：
1. 深度信息整合：融合公网长尾内容与用户私有知识。
2. 人机协同创作：支持可编辑、可回滚、可解释的行程共创过程。
3. 非标场景规划：优先解决复杂约束场景而非标准化热门线路。

## 二、版本目标（对应护城河方向，含社交接入 L0-L3 全链路落地）

1. **知识护城河目标**：将“知识库”从技术能力升级为 C 端高感知功能，形成用户私有旅行资产沉淀。
2. **体验护城河目标**：将 HITL + Agent Checkpoint 能力前台化，建立“可控修改、局部重排、结果可解释”的交互闭环。
3. **场景护城河目标**：补齐非标规划所需的“约束输入 + 冲突检测 + 替代方案”能力。
4. **生态护城河目标**：建立行程导出与分发能力，把 TripNexus 定位为决策上游入口。

## 三、范围定义

### 3.1 本版本纳入范围

1. 旅行灵感收纳箱（Knowledge Base 产品化）
   - 支持更明确的知识来源管理与使用状态感知。
   - 支持在生成行程前设置“本次优先参考的知识资产”。
   - 社交 URL 分层接入能力在 v0.0.6 落地 L0-L3 全链路（含 L2 有限平台适配、L3 浏览器渲染兜底）。
2. 行程约束显式化（Constraints UI + Prompt 注入）
   - 增加预算、体能强度、节奏偏好、家庭友好等约束参数。
   - 约束参数进入主流程并参与生成/修改。
3. 冲突检测与解释（Conflict Check）
   - 针对时间冲突、路程过长、节奏过密给出 warning。
   - 提供 Plan A / Plan B 两套替代建议。
4. 局部重排增强（Replan by Scope）
   - 支持“锁定不改动天数 + 指定单天/半天重排”。
   - 保留原有 Agent 升级与恢复能力。
5. 分享与克隆（Fork & Tweak）
   - 支持结构化行程生成分享链接。
   - 支持基于分享行程继续微调。

### 3.2 本版本不纳入范围

1. 不建设机票/酒店交易闭环，不接入支付与订单系统。
2. 不做全量社交社区，仅落地“分享与克隆”的最小闭环。
3. 不新增重型推荐系统，仅在现有 LLM + RAG + 工具体系上扩展规则与流程。

## 四、需求规划（按能力域）

### 4.1 旅行灵感收纳箱（P0）

#### 4.1.1 用户价值
1. 用户可把碎片化攻略、游记、PDF 变成可复用的个人知识资产。
2. 行程生成时可被“定向引用”，避免 AI 输出泛化模板行程。

#### 4.1.2 功能需求
1. 知识库管理增强
   - 显示知识库来源类型（PDF/Markdown/URL/TXT）。
   - 显示知识条目数量与最后更新时间。
2. 生成链路接入增强
   - `knowledge_base_id` 保持必传可选。
   - 新增 `knowledge_query` 默认值策略（当用户未填写时，自动基于目标地 + 偏好构建查询）。
3. 证据可见性
   - 前端可查看本次使用的 summary/body evidence 摘要。
   - 支持“仅使用私有知识 / 私有+公网融合”开关。

#### 4.1.3 社交数据接入策略

> 详细技术方案见 `docs/技术难点/社交URL内容获取方案.md`

1. 核心策略
   - 按”内容获取方式分层”而非”按平台逐一攻克抓取”。
   - 对开放网页：自动抓取做到稳定。对高风险社交平台：用户辅助导入做到顺滑。
   - 不做反爬对抗能力，不绕过平台访问限制。

2. 平台分级（v0.0.6 落地）
   - **低风险**（博客、媒体、攻略站）：`requests + bs4` 自动提取，高成功率。
   - **中风险**（知乎公开问答、B站）：自动提取 + 质量门禁，失败时引导手动导入。
   - **高风险**（小红书、微博）：用户辅助导入优先，自动抓取仅 best effort。
   - 平台识别范围：`xiaohongshu`、`weibo`、`zhihu`、`bilibili`、`unknown`。

3. 分层获取架构（v0.0.6 落地 L0 + L1 + L2 + L3）
   - **L0 URL 预处理层**：URL 规范化、去追踪参数、短链解跳（`xhslink.com`/`b23.tv`/`t.cn`）、平台识别、风险等级识别。
   - **L1 开放网页自动提取层**：复用现有 `requests + BeautifulSoup`，补齐元信息提取（`og:title`/`og:description`）和质量门禁。
   - **L2 有限平台适配层**（v0.0.6 落地）：仅做最小化元信息 + 公开首屏正文提取，不承诺评论区/多页/登录后内容。
   - **L3 浏览器渲染兜底层**（v0.0.6 落地）：Playwright 仅用于公开页 CSR 补救，不负责绕过登录/风控。

4. 入库前质量门禁（v0.0.6 落地）
   - 新增 `validate_content_quality(content_text, metadata)` 函数。
   - 检查项：最小字符数（`>= 80`）、有效文本密度（`>= 0.15`）、中文内容比例（`>= 0.3`）、重复段比例（`< 0.4`）、噪声模板命中。
   - `quality_score` 0-100，入库阈值 `>= 40`。
   - 去重判定：同 `knowledge_base_id` 下相同 `resolved_url` 已有高相似度条目则拒绝入库。

5. 新增错误码（v0.0.6 落地）
   - `AUTO_PARSE_EMPTY`：有效文本 < 80 字符
   - `AUTO_PARSE_LOW_QUALITY`：quality_score < 40
   - `AUTO_PARSE_DUPLICATED`：重复内容
   - `AUTO_PARSE_BLOCKED`：命中”下载 App”等关键词
   - `AUTO_PARSE_LOGIN_REQUIRED`：命中登录提示关键词
   - `AUTO_PARSE_RISK_VERIFICATION`：命中验证码/人机验证关键词
   - `AUTO_PARSE_PAYWALLED`：命中会员/付费关键词
   - `URL_RESOLVE_LOOP`/`URL_RESOLVE_TIMEOUT`：短链解跳异常

6. 前端交互增强（v0.0.6 落地）
   - URL 输入后、导入前：根据 `source_risk_level` 展示前置引导文案（低风险默认自动、高风险默认手动）。
   - 导入失败后：按 `source_platform` + `ingest_error_code` 展示平台级引导文案，引导用户切换手动导入。
   - 来源列表增加：平台标签（带图标）、风险等级颜色标识、错误码 hover 说明。

7. P1 范围（下版本）
   - 登录态授权导入（仅在用户授权和平台规则允许时启用）。
   - 图文 OCR、短视频字幕抽取等多模态增强。

8. P2 范围（后续）
   - 浏览器插件/书签脚本授权导入。
   - 按 ROI 建设平台专属 connector。

9. 合规边界
   - 不绕过平台访问限制，不破解登录态，不提供反爬对抗策略。
   - 支持数据可追溯、可删除、可过期。


### 4.2 行程约束显式化（P0）

#### 4.2.1 用户价值
1. 用户可直接表达“预算/体力/家庭成员限制”等硬条件。
2. 降低反复口头修正成本，提升首版行程可用度。

#### 4.2.2 功能需求
1. 约束输入面板
   - 预算档位：经济/均衡/舒适。
   - 体能强度：休闲/标准/特种兵。
   - 节奏偏好：文化探索/打卡效率/亲子友好。
   - 特殊约束：步行上限、午休需求、无障碍需求。
2. 约束协议统一
   - 前端将约束归一为结构化对象。
   - 后端在主流程内注入 LLM prompt 与重排行为。
3. 结果可追踪
   - 输出中标记关键约束是否满足（如“已控制单日步行强度”）。
   - 前端展示约束满足状态，后端在主流程内注入 LLM prompt 与重排行为。

#### 4.2.3 对应现有模块
1. 前端：`web/src/components/TripTab.jsx`、`web/src/hooks/useTrip.js`
2. 后端：`src/llm/llm_manager.py`（build_prompt / change_trip）

### 4.3 冲突检测与解释（P0）

#### 4.3.1 用户价值
1. 在不合理行程出现时给出明确原因，而不是直接失败或静默改写。
2. 用户可在“坚持原方案”和“接受替代方案”之间做决策。

#### 4.3.2 功能需求
1. 冲突检测规则
   - 距离与时间冲突（半天跨城、连续超长交通）。
   - 节奏冲突（单日 POI 过密、无休息窗口）。
2. 解释与替代输出
   - 输出冲突原因文本。
   - 输出 Plan A（保守可执行）/Plan B（满足偏好但风险更高）。
3. 流式事件扩展
   - 在 `step` 事件中增加 `warning/conflict` 类型，前端高亮展示。

#### 4.3.3 对应现有模块
1. 后端：`src/api/app.py`（流式事件输出）、`src/llm/llm_manager.py`
2. 前端：`web/src/api/flowApi.js`、`web/src/components/TripTab.jsx`

### 4.4 局部重排与可控共创（P1）

#### 4.4.1 用户价值
1. 用户可只改动某一天或某半天，减少“全局重算导致满意部分被破坏”。
2. 强化用户掌控感，提升多轮编辑体验。

#### 4.4.2 功能需求
1. 锁定机制
   - 支持锁定 day/block，不参与重排。
2. 局部重排
   - 支持“仅重排第 N 天”或“第 N 天下午”。
3. Agent 升级条件联动
   - 当局部重排触发跨天依赖时，自动升级 Agent 并返回触发原因。

#### 4.4.3 对应现有模块
1. 后端：`src/agent/agent_loop.py`、`src/api/app.py`
2. 前端：`web/src/components/TripTab.jsx`、`web/src/components/TripMap.jsx`

### 4.5 分享与克隆（P1）

#### 4.5.1 用户价值
1. 用户可快速把结构化成果外发，形成自然传播。
2. 新用户可在现有方案上二次编辑，降低冷启动门槛。

#### 4.5.2 功能需求
1. 分享
   - 生成可访问链接（包含行程结构与必要摘要）。
2. 克隆
   - 新会话可加载分享方案并继续编辑。
3. 权限与安全
   - 默认匿名分享，不包含用户隐私上下文。

#### 4.5.3 对应现有模块
1. 前端：`web/src/App.jsx`、`web/src/hooks/useTrip.js`
2. 后端：`src/api/app.py`（新增分享/克隆接口）

## 五、非功能需求

1. 性能
   - 常规生成链路时延相对 v0.0.5 增量不超过 20%。
2. 稳定性
   - 冲突检测失败不阻断主流程，自动降级为普通生成并记录 warning。
3. 可观测性
   - 新增指标：`constraint_used`、`conflict_detected`、`plan_alternative_generated`、`share_created`、`fork_started`。
   - 新增社交导入指标：`social_ingest_total`、`social_ingest_success_rate`、`social_ingest_fallback_rate`、`social_ingest_by_platform`（按平台统计）、`social_ingest_by_risk_level`（按风险等级统计）、`quality_gate_reject_rate`（质量门禁拒绝率）。
4. 可维护性
   - 新增字段优先复用现有 `/api/flow/stream` 协议，不新增并列主链路。
5. 合规性
   - 社交链接接入全链路保留来源 metadata 与删除能力，满足最小合规要求。

## 六、验收标准

### 6.1 功能验收

1. 旅行灵感收纳箱可完成创建、上传、选择并参与行程生成。
2. 社交链接导入可完成”解析成功入库”与”解析失败降级导入”两条路径。
   - URL 预处理（短链解跳、平台识别、风险分级）正常工作。
   - 质量门禁可拦截低质量/噪声页面并返回准确错误码。
   - 前端可根据平台和风险等级展示差异化引导文案。
3. 约束参数可被稳定传递并影响行程结果。
4. 冲突场景可返回可解释 warning 与替代方案。
5. 局部重排支持锁定与指定范围改动。
6. 分享链接可打开并支持克隆后二次编辑。

### 6.2 体验验收

1. 用户在 3 次以内编辑中可完成“初版->可执行版”行程收敛。
2. 冲突提示可读且可操作，不出现“只报错不给建议”。
3. 局部重排后，未锁定部分保持最小变更原则。

### 6.3 技术验收

1. 主流程事件协议兼容现有前端消费逻辑。
2. 新增功能不破坏 v0.0.5 的 pause/resume/retry 闭环。
3. 核心接口具备最小回归测试样例（知识库注入、约束注入、冲突警告、局部重排）。

## 七、优先级与排期建议

### P0（本版本必须落地）
1. 旅行灵感收纳箱产品化（可见性 + 使用策略）。
2. 约束输入面板与后端注入链路。
3. 冲突检测与解释输出（含流式 warning 事件）。
4. 社交内容接入网关 P0（通用 URL 解析 + 失败降级 + 来源标注）。

### P1（本版本应做）
1. 局部重排锁定机制。
2. 分享与克隆最小闭环。
3. 社交数据授权导入与多模态增强（OCR/字幕抽取）。

### P2（可延后）
1. 多平台外部导出（Notion/日历）标准化。
2. 逆向比价与分销链接能力。

## 八、版本成功判定（Go / No-Go）

满足以下条件可判定 v0.0.6 达标：
1. P0 功能全部上线并通过自测。
2. 常规链路稳定性不低于 v0.0.5。
3. 至少一个非标场景（如亲子/自驾/高强度）形成可复用演示样例。
4. 用户可感知“我的知识 + 我的约束 + 我的可控修改”三项核心差异化体验。




## 九、开发实施记录

### 9.1 旅行灵感收纳箱（P0）

#### 9.1.1 任务规划（前端 / 后端 / API / 数据结构 / 验收用例）
1. 前端任务规划（P0）
   - 在 `KnowledgeTab.jsx` 提供社交链接导入入口（URL 输入框 + 导入按钮）。
   - 增加导入状态机：解析中、成功、失败、降级建议（手动粘贴/OCR）。
   - 在来源列表展示 `source_type/source_platform/source_url/ingest_status`，并增强可读性（平台标签、风险标识、错误码提示）。
   - 支持失败后一键切换到手动导入模式（文本粘贴 + OCR 文本）。
   - URL 输入后调用预处理接口，按 `source_risk_level` 展示前置引导文案。
   - 自动导入失败后按 `source_platform + ingest_error_code` 展示平台级引导文案。
   - P1 延伸：增加失败来源展示、失败后补全文本重试入口、命中来源与来源总览分区展示、OCR/字幕导入入口。
2. 后端任务规划（P0）
   - 在 `app.py` 落地社交导入编排：参数校验 -> L0 URL 预处理 -> URL 抓取 -> 正文抽取 -> 质量门禁 -> 标准化 -> 入库。
   - 新增 `src/rag/network/url_preprocessor.py`：URL 规范化、去追踪参数、短链解跳、平台识别、风险等级映射。
   - 新增 `src/rag/network/content_validator.py`：`validate_content_quality()` 与质量评分（0-100，阈值 >= 40）。
   - 平台识别补齐 `zhihu.com` / `zhuanlan.zhihu.com`。
   - 引入 L2 有限平台适配层与 L3 浏览器渲染兜底层。
   - 在入库 metadata 中补齐 `normalized_url/resolved_url/source_risk_level/extractor_layer/quality_score` 等字段。
   - 增加来源删除、来源追踪、过期清理标记等合规能力。
   - P1 延伸：失败来源持久化、失败来源原位重试、`source_evidence` 精确命中反查、OCR/字幕文本标准入库编排。
3. API 任务规划（P0）
   - `POST /api/knowledge/bases/{knowledge_base_id}/ingest/url`：支持 `auto/manual` 导入与 fallback 返回。
   - `POST /api/knowledge/preprocess/url`：前端在输入 URL 后预获取平台与风险等级。
   - `GET /api/knowledge/bases/{knowledge_base_id}/sources`：返回来源列表与导入状态统计。
   - `DELETE /api/knowledge/bases/{knowledge_base_id}/sources/{source_id}`：按来源删除分块用于撤回。
   - P1 延伸：`sources` 返回失败来源与更完整错误信息；新增或扩展来源重试接口；`knowledge/search` 与 `flow/stream` 仅返回本次实际命中来源。
4. 数据结构任务规划（P0）
   - metadata 标准字段包含 `source_id/source_type/source_platform/source_url/ingest_mode/ingest_status/ingest_error_code/ingested_at/expires_at`。
   - metadata 扩展字段包含 `normalized_url/resolved_url/source_risk_level/extractor_layer/quality_score`。
   - RAG 检索结果透传 `source_type/source_platform/source_url/ingest_status` 用于前端证据展示。
   - P1 延伸：补齐 `failure_reason/last_retry_at/retry_count/hit_chunk_ids/hit_count` 等字段。
5. 验收用例规划（P0）
   - 解析成功：公开 URL 导入成功，`ingest_status=parsed`，可被检索命中。
   - 解析失败降级：自动解析失败后，手动文本补全成功，`ingest_status=fallback`。
   - metadata 完整性：来源字段可查询且前端列表可见。
   - 删除与撤回：删除 `source_id` 后对应分块不再被召回。
   - 合规边界：受限链接返回明确错误码，不触发绕过策略。
   - 平台分级引导、质量门禁、短链解跳、去重判定、L2/L3 提取路径均有回归用例。
   - P1 延伸：失败来源进入列表与统计；失败来源保留原 `source_id` 重试成功；`source_evidence` 仅展示实际命中来源；OCR/字幕文本导入可被私有检索命中。

#### 9.1.2 实际完成情况（截至当前代码）
**完成度概览**
- 已完成：URL 导入主链路、手动降级导入、来源列表聚合展示、按 `source_id` 删除、来源字段透传到知识检索与主流程调试区域。
- 已完成：`/api/knowledge/preprocess/url` 与 L0 URL 预处理（规范化/短链解跳/平台识别/风险分级）、L2/L3 提取层接入、metadata 扩展字段落库。
- 已完成：失败来源持久化、失败来源在来源列表中展示、失败来源原位补全文本重试、`source_evidence` 精确命中透传、OCR/字幕文本最小导入入口。
- 已完成：质量门禁（`quality_score` + 错误码）已同时接入 `ingest/url` 自动解析链路与 `POST /api/knowledge/preprocess/url` 预处理链路。前端在“输入 URL 但尚未导入”阶段即可拿到 `extractor_layer/quality_score/ingest_error_code/failure_reason/parsed_content_preview/parsed_content_chars/requires_user_assist`，可以提前判断是否值得走自动解析。
- 已完成：L2/L3 提取层从“最小可用”推进到“增强版 best effort”。当前 L1/L2 会补抽 `title/description/author/published_at/tags` 等元信息，L2 扩大了 `zhihu/bilibili/xiaohongshu/weibo` 的正文 selector 覆盖；L3 会在 Playwright 公开页兜底前尝试轻量滚动与常见“展开全文/显示更多”按钮点击，再回落到通用 HTML 解析。

**实现逻辑说明**
1. 导入编排主链路（已完成）
   - `POST /api/knowledge/bases/{knowledge_base_id}/ingest/url` 已落地 `auto/manual` 双模式：
   - `manual`：直接使用 `manual_text/ocr_text` 入库，状态为 `fallback`。
   - `auto`：调用 `ContentCrawler.fetch_url_with_fallback()`，按 L1 HTML 提取 -> L2 平台选择器 -> L3 浏览器兜底的顺序抓取正文；自动解析成功且通过质量门禁时为 `parsed`，失败时若携带手动文本则转为 `fallback`，否则返回 `failed`。
2. 预处理预判与来源 metadata（已完成）
   - `POST /api/knowledge/preprocess/url` 现已在 URL 预处理后复用自动解析链路，返回 `extractor_layer/quality_score/ingest_error_code/failure_reason/content_lang/requires_user_assist/parsed_content_preview/parsed_content_chars`。
   - `ingest/url` 与 `preprocess/url` 共享同一套自动解析与质量门禁 helper，减少“预处理提示可导入，但真实导入失败”的判断漂移。
   - 入库前统一调用 `_build_source_metadata()` 生成来源字段。
   - 来源查询通过 `_load_collection_source_entries()` 合并“已入库分块来源 + registry 中的失败来源记录”，输出完整的 `parsed/fallback/failed` 统计。
   - 自动解析失败后会将失败来源写入 registry，保证来源列表、统计与后续重试链路可见。
3. 删除与撤回能力（已完成）
   - 删除接口按 `source_id` 查找关联 `chunk_ids`，执行向量库物理删除，形成按来源撤回闭环。
4. 前端导入交互（已完成）
   - 已提供 URL 输入、模式切换、失败后显示手动输入区、导入状态标签（解析中/成功/降级/失败）与失败建议提示。
   - 已增加 OCR/字幕文本输入区；来源列表已展示 `source_type/source_platform/source_url/ingest_status/chunks_count`、错误码，并支持删除与失败来源重试。
5. 主流程证据来源展示（已完成）
   - 前端可在知识检索与主流程调试区域看到 `source_type/source_platform/source_url/ingest_status`。
   - 当前 `source_evidence` 已改为基于实际命中文档反查来源，只展示“本次实际命中的来源集合”。
6. 失败后修复与重试（已完成）
   - 失败来源可保留原 `source_id` 展示在来源列表中，并通过“补全文本并重试”入口转为 `fallback`。
   - 重试成功后会清理失败来源记录，保持来源追踪与检索引用稳定。
7. P1 需求同步状态（已完成）
   - 9.1.4 中定义的失败来源闭环、失败来源重试、命中来源精确透传、OCR/字幕入口已在当前代码中落地。

**对应文件位置**
1. 前端
   - `web/src/components/KnowledgeTab.jsx`：社交链接导入 UI、失败降级输入区、OCR/字幕输入区、来源列表、失败来源重试与来源删除交互。
   - `web/src/hooks/useKnowledge.js`：导入请求编排、来源列表刷新、状态统计与删除操作。
   - `web/src/api/knowledgeApi.js`：`ingest/url`、`sources` 查询与删除 API 封装。
2. 后端
   - `src/api/app.py`：
     - `ingest_knowledge_url`：URL 导入主链路与 fallback 判定。
     - `_run_url_auto_parse_preview`：预处理试解析与正式导入共享的自动解析/质量门禁 helper。
     - `_load_failed_source_entries/_upsert_failed_source_entry/_delete_failed_source_entry`：失败来源持久化与清理。
     - `preprocess_knowledge_url`：URL 预处理接口（平台与风险前置判断 + 试解析预判）。
     - `_infer_source_platform`：平台识别（当前 xiaohongshu/weibo/zhihu/bilibili/unknown）。
     - `_build_source_metadata`：来源 metadata 组装（含 normalized/resolved/risk/layer/quality）。
     - `list_knowledge_sources` / `_load_collection_source_entries`：来源聚合与状态统计（含失败来源合并）。
     - `update_knowledge_source`：失败来源补全文本重试与原位修复。
     - `delete_knowledge_source`：按来源删除向量分块，并清理失败来源记录。
     - `_build_source_evidence_from_docs`：基于实际命中文档回传精确来源。
3. RAG 侧
   - `src/rag/network/crawler.py`：L1/L2/L3 分层抓取能力（含元信息抽取、平台 selector 扩展、L3 轻量展开/滚动兜底，供 `ingest_knowledge_url` 与 `preprocess_knowledge_url` 复用）。
   - `src/rag/network/url_preprocessor.py`：URL 规范化、短链解跳、平台识别、风险分级。
   - `src/rag/network/content_validator.py`：质量门禁与错误码判定。

#### 9.1.3 自测流程（已完成功能）
1. 启动与准备
   - 启动后端与前端，打开 Knowledge 页面。
   - 新建一个知识库，记录 `knowledge_base_id`。

2. 已验证测试样例（可直接复用）
   - 景点主题：杭州西湖 3 天 2 夜。
   - 旅游社交链接（知乎）：`https://zhuanlan.zhihu.com/p/2005664589942108517`
   - 本地代码预验证结果（已跑通）：
   - `source_platform=zhihu`、`source_risk_level=medium`
   - 抓取层级：`extractor_layer=l1_html`
   - 正文长度：`content_len=1310`
   - 正文命中关键词示例：`西湖`、`西溪湿地`、`断桥`、`灵隐寺`、`龙井村`

3. URL 预处理验证（前端）
   - 在「社交链接导入」输入已验证链接：`https://zhuanlan.zhihu.com/p/2005664589942108517`。
   - 通过标准：输入框下方出现 `平台 zhihu`、`风险 medium`，并在命中正文时看到 `layer/quality/chars` 标签与“预处理正文预览”；若预判失败，可直接看到 `error` 标签与手动导入建议。

4. 自动导入验证（前端）
   - 保持模式为「自动解析」，点击「导入社交内容」。
   - 通过标准：状态标签出现 `解析成功` 或 `降级成功`；出现“本次解析正文预览”；来源列表新增一条记录。

5. 手动导入验证（前端）
   - 切换到「手动导入」，在“手动粘贴正文”填入一段 100 字以上文本后导入。
   - 通过标准：状态标签为 `降级成功`；来源列表新增 `source_type=manual`（或 `ocr`）记录，且有 `chunks` 数。

6. 来源列表与元数据验证（前端）
   - 在「当前知识库来源列表」查看刚导入的记录。
   - 通过标准：可见 `source_type/source_platform/ingest_status/chunks`，并可看到 `risk/layer/quality` 标签（有值时显示）；失败来源可见 `error` 标签。

7. 删除来源验证（前端）
   - 在来源列表点击某条记录右侧「删除」，并在确认框确认。
   - 通过标准：该条来源从列表消失，顶部统计（总数/parsed/fallback/failed）同步变化。

8. 检索与透传验证（前端）
   - 在「检索知识库」中选择 `仅使用私有知识`，输入测试问题（例如“西湖三天路线怎么排”）并点击「开始检索」。
   - 通过标准：下方出现检索结果；「私有知识命中调试」区域出现 `scope/kb_context_count/source_evidence_count`，并明确标注“仅展示本次实际命中来源”；来源条目带 `source_type/source_platform/ingest_status/source_url`。

9. 后端补充校验（仅补充，不替代前端）
   - 预处理返回值：调用 `POST /api/knowledge/preprocess/url`，确认除前端展示的 `source_platform/source_risk_level` 外，还返回 `normalized_url/resolved_url/resolve_error_code/extractor_layer/quality_score/ingest_error_code/failure_reason/parsed_content_preview/parsed_content_chars/requires_user_assist`。
   - 来源落库字段：调用 `GET /api/knowledge/bases/{knowledge_base_id}/sources`，确认 `normalized_url/resolved_url/source_risk_level/extractor_layer/quality_score` 已真实落库。
   - 删除一致性：前端删除来源后，再调用一次 `GET /api/knowledge/bases/{knowledge_base_id}/sources`，确认该 `source_id` 已不存在。
   - 失败错误码：构造一个失败导入场景后，查看 `POST /api/knowledge/bases/{knowledge_base_id}/ingest/url` 响应中的 `metadata.ingest_error_code` 是否符合预期。

10. 9.1.4 新增能力自测项（当前代码已支持）
- 失败来源验证：自动导入失败后，来源列表应新增失败记录，且顶部 `failed` 统计增加。
- 失败来源重试验证：在失败来源上补充手动文本后重试，原 `source_id` 不变，状态更新为 `fallback` 或 `parsed`。
- 精确命中验证：执行知识检索或主流程后，调试区 `source_evidence` 仅展示本次实际命中的来源，不再展示无关来源样本。
- 多模态导入验证：通过 OCR 或字幕入口导入文本后，来源列表可见对应记录，且能被私有检索命中。
11. 当前已知边界（按现状判定）
- `preprocess/url` 现在会做一次无副作用的“试解析”，但不会做去重判定、失败来源落库或真正入库；它的作用是预判，不是替代 `ingest/url`。
- 预处理阶段拿到的 `quality_score`、`extractor_layer` 与失败码属于 best effort 结果；若目标页面在短时间内发生内容变化、风控升级或重定向变化，实际导入结果仍可能不同。
- L2/L3 的目标是提高公开页面正文命中率，不保证平台级完整还原；遇到需要登录、强风控、评论区/楼层、深层懒加载、多段展开或复杂页面结构时，仍应优先引导用户切换手动/OCR 补录。


### 9.2 行程约束显式化（P0）

#### 9.2.1 任务规划（前端 / 后端 / API / 数据结构 / 验收用例）

1. 前端任务规划（P0）
   - 在 `App.jsx` 的行程生成表单（当前 destination/days/budget/preference 四字段）中新增「约束输入面板」折叠区域，包含：
     - **预算档位**：三选一（经济/均衡/舒适），替代当前自由文本 `budget` 字段，或作为结构化补充（保留原 budget 输入兼容旧逻辑）。
     - **体能强度**：三选一（休闲/标准/特种兵），影响单日 POI 数量、步行距离与行程节奏。
     - **节奏偏好**：三选一（文化探索/打卡效率/亲子友好），影响景点类型与时间分配。
     - **特殊约束**（可选，多选/自由输入）：步行上限（km）、午休需求（是/否）、无障碍需求（是/否）。
   - 约束面板默认折叠，展开时显示上述选项；未展开时使用默认值（均衡/标准/文化探索/无特殊约束）。
   - `handleFlowFormSubmit()` 中将约束字段归一为 `constraints` 结构化对象，追加到 payload。
   - 在 `TripTab.jsx` 的行程结果区域增加「约束满足状态」展示区：
     - 显示本次生成所使用的约束参数摘要（如"预算：经济 | 强度：休闲 | 节奏：亲子友好"）。
     - 若后端返回约束满足/违反标记，高亮展示（如 ✓ 已控制步行强度 / ⚠ 单日景点偏多）。
   - 在修改行程（`change_trip`）时，约束参数应随修改请求一并传递，确保局部修改仍遵循原始约束。

2. 后端任务规划（P0）
   - 在 `src/api/app.py` 的 `FlowStreamRequest` 模型中新增可选字段：
     - `budget_level: Optional[str]`（"economy" / "balanced" / "comfortable"，默认 "balanced"）
     - `intensity: Optional[str]`（"leisure" / "standard" / "extreme"，默认 "standard"）
     - `pace: Optional[str]`（"cultural" / "efficient" / "family_friendly"，默认 "cultural"）
     - `special_constraints: Optional[dict]`（`{"walking_limit_km": float, "need_nap": bool, "accessibility": bool}`）
   - 在 `_run_flow_stream()` 中提取约束参数，传入 `build_prompt()` 和 `change_trip()`。
   - 在 `src/llm/llm_manager.py` 的 `build_prompt()` 中扩展 `【行程约束】` 模板区块：
     - 当前模板已有 destination/days/budget/preference/edit_note。
     - 新增约束段落，将结构化约束转为自然语言指令注入 prompt，例如：
       ```
       - 预算档位：经济（优先公共交通与平价餐饮，控制总花费）
       - 体能强度：休闲（单日步行不超过 8000 步，每日安排不超过 3 个主要景点）
       - 节奏偏好：亲子友好（优先亲子景点、避免过早出发、预留午休时间）
       - 特殊约束：步行上限 5km/天、需要午休、需要无障碍通道
       ```
     - 预算档位与强度的量化映射表内置于 `llm_manager.py`（如 leisure → 单日 ≤3 POI、walking ≤ 5km；extreme → 单日 ≥6 POI）。
   - 在 `change_trip()` 中同步注入约束参数，确保修改行程时约束持续生效。
   - 在 finalize 阶段的事件 payload 中增加 `constraints_used` 字段，返回本次实际使用的约束参数与满足状态。

3. API 任务规划（P0）
   - **`POST /api/flow/stream` 请求扩展**（无新增端点，复用现有协议）：
     ```json
     {
       "destination": "杭州",
       "days": 3,
       "budget": "3000",
       "preference": "自然风光",
       "budget_level": "economy",
       "intensity": "leisure",
       "pace": "family_friendly",
       "special_constraints": {
         "walking_limit_km": 5,
         "need_nap": true,
         "accessibility": false
       },
       "knowledge_base_id": "...",
       "knowledge_scope": "private_only"
     }
     ```
   - **流式事件扩展**（finalize 阶段 payload）：
     ```json
     {
       "event": "end",
       "step": "finalize",
       "payload": {
         "trip_data": { ... },
         "constraints_used": {
           "budget_level": "economy",
           "intensity": "leisure",
           "pace": "family_friendly",
           "special_constraints": { "walking_limit_km": 5, "need_nap": true, "accessibility": false }
         },
         "constraints_satisfied": [
           { "label": "已控制单日步行强度", "status": "met" },
           { "label": "已安排午休时间段", "status": "met" },
           { "label": "单日景点数量 ≤ 3", "status": "met" }
         ]
       }
     }
     ```
   - **`POST /api/flow/change` 请求扩展**（修改行程时携带约束）：
     - 在修改请求中增加可选 `constraints` 字段，结构同上，确保修改后的行程仍满足约束。
   - **向后兼容**：所有约束字段均为 Optional，未传时使用默认值，不影响现有调用方。

4. 数据结构任务规划（P0）
   - **约束输入结构**（前端 → 后端）：
     ```python
     class SpecialConstraints(BaseModel):
         walking_limit_km: Optional[float] = None   # 每日步行上限（公里）
         need_nap: bool = False                      # 是否需要午休
         accessibility: bool = False                 # 是否需要无障碍

     class TripConstraints(BaseModel):
         budget_level: str = "balanced"              # economy / balanced / comfortable
         intensity: str = "standard"                 # leisure / standard / extreme
         pace: str = "cultural"                      # cultural / efficient / family_friendly
         special_constraints: Optional[SpecialConstraints] = None
     ```
   - **约束满足状态结构**（后端 → 前端）：
     ```python
     class ConstraintStatus(BaseModel):
         label: str        # 约束描述（如"已控制单日步行强度"）
         status: str       # "met" / "partially_met" / "violated"
         detail: Optional[str] = None  # 补充说明（如"第2天步行约 6.2km，略超上限"）
     ```
   - **Prompt 注入映射表**（内置于 `llm_manager.py`）：
     ```python
     BUDGET_LEVEL_MAP = {
         "economy": "经济模式（优先公共交通与平价餐饮，避免高消费景点门票，总花费控制在较低水平）",
         "balanced": "均衡模式（交通与餐饮灵活搭配，景点门票适度）",
         "comfortable": "舒适模式（可选打车/专车，推荐特色餐厅，不限制高价景点）"
     }
     INTENSITY_MAP = {
         "leisure": "休闲模式（单日不超过 3 个主要景点，步行控制在 5km 内，安排充裕休息时间）",
         "standard": "标准模式（单日 4-5 个景点，步行 8-12km，节奏适中）",
         "extreme": "特种兵模式（单日 6+ 个景点，可接受高强度步行与早出晚归，追求最大覆盖）"
     }
     PACE_MAP = {
         "cultural": "文化探索（偏向博物馆、历史遗迹、文化体验，每个景点预留充足时间）",
         "efficient": "打卡效率（热门景点快速打卡，紧凑安排，最大化游览数量）",
         "family_friendly": "亲子友好（优先亲子乐园、自然公园等儿童友好景点，避免过早出发，预留午休时间）"
     }
     ```
   - **与现有 budget/preference 字段的关系**：
     - `budget`（自由文本）保留，作为预算金额参考；`budget_level` 为结构化档位，两者互补。
     - `preference`（自由文本/列表）保留，约束面板为结构化补充，不替代自由表达。

5. 验收用例规划（P0）
   - **用例 1：默认约束生成**
     - 不填写约束面板（使用默认值），生成行程。
     - 通过标准：行程正常生成，finalize 事件中 `constraints_used` 包含默认值，与 v0.0.5 行为兼容。
   - **用例 2：经济 + 休闲约束生成**
     - 设置预算档位=经济、体能强度=休闲、节奏偏好=亲子友好。
     - 通过标准：生成行程中单日景点 ≤ 3 个，交通方式以公共交通为主，景点类型偏向亲子友好，`constraints_satisfied` 各项 status 为 "met"。
   - **用例 3：特种兵模式约束生成**
     - 设置体能强度=特种兵、节奏偏好=打卡效率。
     - 通过标准：生成行程中单日景点 ≥ 5 个，节奏紧凑，覆盖热门打卡点。
   - **用例 4：特殊约束生效**
     - 设置步行上限=5km、需要午休=是。
     - 通过标准：LLM prompt 中包含步行上限与午休要求，生成行程中每日安排有午休时段（12:00-14:00 区间有休息安排）。
   - **用例 5：约束在修改行程时持续生效**
     - 先以"经济+休闲"约束生成行程，再通过 `change_trip` 修改某天景点。
     - 通过标准：修改后的行程仍遵循经济与休闲约束，不出现高消费景点或高强度安排。
   - **用例 6：约束满足状态展示**
     - 设置约束后生成行程，在 `TripTab` 查看约束状态区域。
     - 通过标准：前端展示约束参数摘要与各项满足状态标记。
   - **用例 7：向后兼容验证**
     - 使用 v0.0.5 的请求格式（不带任何约束字段）调用 `/api/flow/stream`。
     - 通过标准：行程正常生成，无报错，默认约束值生效。
   - **用例 8：约束参数传递完整性验证（后端）**
     - 调用 `/api/flow/stream` 携带完整约束参数，在后端日志中确认 `build_prompt()` 接收到的约束参数与请求一致。
     - 通过标准：prompt 中包含对应的约束描述文本（如"经济模式"、"休闲模式"、"亲子友好"等）。

#### 9.2.2 对应文件位置（预期修改清单）

1. 前端
   - `web/src/App.jsx`：在行程生成表单 Modal 中新增约束输入面板（折叠区域），包含预算档位、体能强度、节奏偏好、特殊约束选项；`handleFlowFormSubmit()` 中组装 `constraints` 对象。
   - `web/src/components/TripTab.jsx`：在行程结果区域新增「约束满足状态」展示区，读取 finalize 事件中的 `constraints_used` 和 `constraints_satisfied` 渲染摘要与状态标记。
   - `web/src/hooks/useTrip.js`：`handleFlowSubmit()` 中从 values 提取约束字段，追加到 payload；`handleChangeTrip()` 中同步传递当前约束。
   - `web/src/api/flowApi.js`：无需修改（通用 JSON 透传）。

2. 后端
   - `src/api/app.py`：
     - `FlowStreamRequest` 模型新增 `budget_level / intensity / pace / special_constraints` 可选字段。
     - `_run_flow_stream()` 中提取约束参数，传入 `build_prompt()` 与 `change_trip()`。
     - finalize 阶段事件 payload 中追加 `constraints_used` 与 `constraints_satisfied`。
   - `src/llm/llm_manager.py`：
     - 新增 `BUDGET_LEVEL_MAP / INTENSITY_MAP / PACE_MAP` 映射表。
     - `build_prompt()` 扩展 `【行程约束】` 模板区块，将结构化约束转为自然语言指令。
     - `change_trip()` 同步注入约束参数。
     - 新增 `_build_constraint_prompt_section(constraints)` 辅助函数，统一约束文本生成逻辑。

3. 数据模型
   - `src/api/app.py`（或独立 `src/models/constraints.py`）：新增 `SpecialConstraints`、`TripConstraints`、`ConstraintStatus` Pydantic 模型。

#### 9.2.3 当前实际完成情况（截至当前代码）

**完成度概览**
- 已完成：前端行程输入弹窗新增「约束设置」折叠面板，支持预算档位、体能强度、节奏偏好、步行上限、午休、无障碍输入；未展开时使用默认值 `balanced/standard/cultural/无特殊约束`。
- 已完成：`POST /api/flow/stream` 已支持 `budget_level / intensity / pace / special_constraints` 四组约束输入，且保持 Optional 向后兼容。
- 已完成：主流程 `_run_flow_stream()` 已将约束参数归一化后注入 `user_input`，传入行程生成 prompt，并在修改行程 `change_trip()` 时继续透传，保证局部修改尽量延续原始约束。
- 已完成：`llm_manager.py` 已新增 `BUDGET_LEVEL_MAP / INTENSITY_MAP / PACE_MAP`，并通过 `_build_constraint_prompt_section()` 将结构化约束转为自然语言约束段落注入 `【行程约束】`。
- 已完成：finalize 终态事件 payload 已返回 `constraints_used` 与 `constraints_satisfied`；同时也会把这两个字段写入 `trip_data` 本身，便于前端结果页和会话持久化直接复用。
- 已完成：`TripTab.jsx` 已新增「约束满足状态」展示区，展示约束摘要和满足状态标签。
- 已完成：前端修改行程相关链路已显式携带 `constraints`，当前覆盖 `trip/update`（本地编辑/拖拽/删除后的持久化）和 `trip/replan_day`（单日重排）两个入口，不再只依赖后端从 `current_trip.constraints_used` 隐式继承。
- 已完成：当前版本的 `constraints_satisfied` 已落地为后端启发式规则判定，不再依赖 LLM 自评。

**未完成 / 尚未单独落地**
- 未单独落地：文档中提到的 `POST /api/flow/change` 独立接口当前仓库里仍不存在，本次实现复用的是既有主流程修改链路和 `llm_manager.change_trip()`，而不是新增一个独立 change API。
- 未完成：约束判定仍是轻量启发式估算，例如步行距离通过“步行段数量 × 经验系数”估算，不是地图级精确路径计算。

**实现逻辑说明**
1. 前端约束输入与请求组装（已完成）
   - [web/src/App.jsx](/Users/wcbbcc/my-code/my_github/TripNexus/web/src/App.jsx) 的 `DEFAULT_TRIP_CONSTRAINTS`、`buildConstraintSummary()`、`handleFlowFormSubmit()` 已在行程输入 Modal 中接入折叠式「约束设置」区域和约束摘要写入逻辑。
   - 默认值定义在 `DEFAULT_TRIP_CONSTRAINTS`，包含：
     - `budget_level = balanced`
     - `intensity = standard`
     - `pace = cultural`
     - `walking_limit_km = null`
     - `need_nap = false`
     - `accessibility = false`
   - 表单提交时 `handleFlowFormSubmit()` 会保留原有 `destination/days/budget/preference`，同时将当前约束摘要写入聊天区，方便用户和调试人员确认本次生成使用了什么约束。
   - [web/src/hooks/useTrip.js](/Users/wcbbcc/my-code/my_github/TripNexus/web/src/hooks/useTrip.js) 中的 `normalizeFlowConstraints()` 和 `handleFlowSubmit()` 已把表单值规整后透传给 `/api/flow/stream`：
     ```json
     {
       "budget_level": "balanced|economy|comfortable",
       "intensity": "leisure|standard|extreme",
       "pace": "cultural|efficient|family_friendly",
       "special_constraints": {
         "walking_limit_km": 5,
         "need_nap": true,
         "accessibility": false
       }
     }
     ```
   - [web/src/hooks/useTrip.js](/Users/wcbbcc/my-code/my_github/TripNexus/web/src/hooks/useTrip.js) 中的 `resolveTripConstraints()`、`persistFlowTripResult()`、`handleFlowReplanDay()` 已将当前行程约束显式透传给 `/api/trip/update` 和 `/api/trip/replan_day`。
2. 后端数据结构与归一化（已完成）
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `FlowRequestBase`、`FlowStreamRequest`、`SpecialConstraints`、`TripConstraints`、`ConstraintStatus` 已完成约束相关数据结构定义：
     - `FlowRequestBase` / `FlowStreamRequest` 已新增 `budget_level / intensity / pace / special_constraints`
     - 新增 `SpecialConstraints`、`TripConstraints`、`ConstraintStatus` 三个 Pydantic 模型
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `_normalize_trip_constraints()`：
     - 对预算档位、强度、节奏做枚举兜底
     - 对 `walking_limit_km` 做浮点数解析和范围裁剪
     - 统一产出结构化 `TripConstraints`
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `_build_flow_query_text()` 也会把预算档位、体能强度、节奏偏好拼入主流程查询文本，方便意图识别与日志观察阶段识别当前任务约束。

3. Prompt 注入与修改行程续传（已完成）
   - [src/llm/llm_manager.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/llm/llm_manager.py) 中的 `BUDGET_LEVEL_MAP`、`INTENSITY_MAP`、`PACE_MAP` 已新增：
     - `BUDGET_LEVEL_MAP`
     - `INTENSITY_MAP`
     - `PACE_MAP`
   - [src/llm/llm_manager.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/llm/llm_manager.py) 中的 `build_prompt()` 的 `【行程约束】` 段落已扩展为：
     - 原始自由输入：`budget / preference`
     - 结构化约束：预算档位、体能强度、节奏偏好、特殊约束
   - [src/llm/llm_manager.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/llm/llm_manager.py) 中的 `_build_constraint_prompt_section()` 负责把结构化约束转成自然语言说明，例如：
     - 经济模式：优先公共交通与平价餐饮
     - 休闲模式：单日不超过 3 个主要景点
     - 亲子友好：避免过早出发并预留午间缓冲
     - 特殊约束：步行上限、午休、无障碍
   - [src/llm/llm_manager.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/llm/llm_manager.py) 中的 `change_trip()` 已新增 `constraints` 参数。
   - [src/llm/llm_manager.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/llm/llm_manager.py) 中的 `_handle_trip_modification()` / `prepare_trip_request_from_modification()` 已支持优先使用：
     1. 调用侧传入的 `constraints`
     2. 当前行程 `current_trip.constraints_used`
     3. 默认值
   - 因此当前“修改行程仍遵循原始约束”的逻辑已经在后端链路中生效。

4. finalize 回传与约束满足状态（已完成）
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `_run_flow_stream()` 在进入生成前先通过 `_normalize_trip_constraints()` 得到 `constraints_used`，并写入 `user_input`。
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `_build_constraint_statuses()` 会在行程生成成功后基于 `trip_data` 和 `constraints_used` 做启发式判定，输出 `constraints_satisfied`。
   - 当前判定逻辑包括：
     - 预算：公共交通/步行占比、是否大量出现打车/专车
     - 强度：单日主要景点数量
     - 节奏：最早出发时间、是否更偏文化/亲子/高效率
     - 步行上限：以步行段数量估算单日步行公里数
     - 午休：是否识别出 12:00-14:00 区间的休息/午休/酒店休整
     - 无障碍：是否在 note/tags/recommend_reason 中识别到无障碍相关词
   - finalize 终态事件 payload 现已包含：
     - `trip_data`
     - `constraints_used`
     - `constraints_satisfied`
   - 同时后端还会将 `constraints_used / constraints_satisfied` 写回 `trip_data`，确保：
     - 会话持久化后仍可展示
     - `TripTab` 直接从 `tripResult` 读取即可
   - [src/api/app.py](/Users/wcbbcc/my-code/my_github/TripNexus/src/api/app.py) 中的 `update_trip()`、`replan_trip_day()` 也已支持接收前端显式透传的 `constraints`，并在保存或单日重排后重新写回 `constraints_used / constraints_satisfied`。

5. 前端结果展示（已完成）
   - [web/src/components/TripTab.jsx](/Users/wcbbcc/my-code/my_github/TripNexus/web/src/components/TripTab.jsx) 中的 `buildConstraintSummary()`、`getConstraintStatusColor()` 和 `TripTab()` 已新增约束状态展示区域。
   - 展示内容分为两部分：
     - 约束摘要：例如 `预算：经济 | 强度：休闲 | 节奏：亲子友好 | 步行上限：5km`
     - 满足状态标签：根据 `met / partially_met / violated` 显示绿色 / 金色 / 红色 Tag
   - 当前展示数据直接来源于 `tripResult.constraints_used` 和 `tripResult.constraints_satisfied`，不依赖额外一次接口请求。

#### 9.2.4 当前建议自测流程

1. 默认约束验证
   - 不展开约束面板，填写目的地=杭州、天数=3，直接生成。
   - 通过标准：
     - 请求成功
     - finalize payload 中存在 `constraints_used`
     - 默认值为 `balanced / standard / cultural`
     - `TripTab` 中可见默认约束摘要

2. 经济 + 休闲 + 亲子友好验证
   - 设置预算档位=经济、体能强度=休闲、节奏偏好=亲子友好。
   - 通过标准：
     - 后端日志中 prompt 包含“经济模式 / 休闲模式 / 亲子友好”
     - finalize payload 存在 `constraints_satisfied`
     - `TripTab` 可见对应状态标签

3. 特殊约束验证
   - 设置步行上限=5km、需要午休=是。
   - 通过标准：
     - prompt 中出现步行上限与午休说明
     - finalize payload 中可看到“已控制单日步行强度”“已安排午休时间段”相关状态

4. 修改行程约束续传验证
   - 先生成一份带约束的行程，再走现有修改链路。
   - 通过标准：
     - 修改后的 `trip_data` 仍保留 `constraints_used`
     - 后端修改链路未丢失约束字段

5. 向后兼容验证
   - 使用旧请求格式调用 `/api/flow/stream`，不带任何约束字段。
   - 通过标准：
     - 请求不报错
     - 后端自动补默认值
     - 前端仍可展示默认约束摘要

#### 9.2.5 当前边界与剩余事项

1. 已知边界
   - `constraints_satisfied` 当前是后端启发式估算，不是地图/路网精确计算。
   - 步行距离使用“步行段数量 × 经验系数”估算，只适合 P0 可观察性，不适合做严谨导航级校验。
   - 无障碍、亲子友好、文化探索等约束主要依赖 `tags / recommend_reason / note / attraction` 文本命中，属于 best effort。
   - 约束目前只影响 prompt 和结果判定，不会改变底层 POI 检索排序逻辑。

2. 剩余待补
   - 文档中提到的 `POST /api/flow/change` 独立接口尚未新增。
   - 前端尚未形成独立的“修改行程请求携带 constraints”的专门接口层；当前依赖后端从既有 `trip_data.constraints_used` 继承。
   - 若后续要提升准确性，可考虑接入真实地图步行距离估算、景点类别标签化和无障碍 metadata。
