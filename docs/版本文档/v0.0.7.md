# v0.0.7 版本发布规划

## 一、版本定位

v0.0.7 定位为 **“产品打磨与生产环境就绪”** 版本。
本版本的核心侧重于产品完整性的最终校验（横向对比）、用户体验的精细化提升、底层用户资产（Token）的管理与监控，以及最终生产环境的平稳落地。致力于让产品不仅“好用”，还能实现“可管、可控、可观测”，为后续的用户规模增长打下坚实的基础。

## 二、版本目标

> 注：下面`1. 竞品对标与功能查漏补缺 (Feature Completeness)`和`2. 用户体验与 UI 优化 (UI/UX Optimization)`依赖手工自测，这里不需要进行任务规划

### 1. 竞品对标与功能查漏补缺 (Feature Completeness)
* **核心链路梳理**：横向对比主流竞品（明确 2-3 个对标产品），梳理核心用户路径，输出功能差异矩阵。
* **遗漏功能补齐**：针对竞品具备且属于我们产品核心价值闭环内的缺失功能，进行紧急评估和补充开发。
* **边缘场景覆盖**：检查边界条件和异常场景的处理逻辑（如断网、无权限、数据为空时的占位图等），确保功能逻辑的严密性。

### 2. 用户体验与 UI 优化 (UI/UX Optimization)
* **视觉一致性校准**：统一全局的色彩规范、字体排版、组件圆角与间距，确保页面风格的高度统一。
* **交互细节打磨**：
    * 完善各类操作的反馈机制（如按钮点击 Loading 态、成功/失败的 Toast 提示、平滑的过渡动画）。
    * 优化复杂表单或数据录入的校验逻辑，提供更友好的错误引导。
* **响应式与适配性**：针对目标用户常用的设备分辨率进行 UI 适配复测，消除排版错乱、文字截断等视觉 Bug。

### 3. 用户管理系统建设 (User Management System)
* **基础账号体系闭环**：完善用户的注册、登录、密码找回/重置等基础功能（或第三方 OAuth 接入），确保账号安全性。
* **后台管理看板 (Admin Panel)**：
    * **用户列表与状态**：支持后台查看注册用户列表、注册时间、基础信息，并提供账号封禁/解封等基础管理操作。
    * **额度与权限控制**：支持在后台为不同用户配置基础的 Token 额度上限，或设置特定功能的访问权限（为后续商业化或分级运营做准备）。

### 4. 线上监控与可观测性建设 (Monitoring & Observability)
* **用户运行情况监控 (User Activity & Operations)**：
    * 核心埋点与请求时间线：记录用户登录、核心功能调用等关键行为，便于排查用户侧反馈的客诉问题。
    * 接口频率限制 (Rate Limiting)：防止恶意刷量或高频并发导致系统崩溃。
* **Token 消耗与成本监控 (Token & Cost Monitoring)**：
    * **颗粒度统计**：建立基于“全局”和“单用户”双维度的大模型 API 请求 Token 消耗统计表。
    * **预警与熔断机制**：设置平台总 Token 余额预警（如低于 20% 通知管理员），以及单用户额度耗尽时的熔断阻断与前端友好提示。
* **系统运行状态监控 (System Health)**：
    * 核心服务健康检查 (Health Check)、基础指标监控（CPU/内存），以及前端/后端的全局异常日志捕获与实时告警。

### 5. 线上环境部署与发布 (Deployment & Release)
* **部署流程规范化**：完成测试环境与生产环境的隔离，梳理并固化从代码合并到上线的标准操作流程（SOP）。
* **自动化构建 (CI/CD)**：配置流水线，实现代码打包、镜像构建与容器部署的自动化，减少人工干预与出错率。
* **高可用与安全预案**：
    * 确认数据库（尤其是业务数据与 Token 消耗数据）的定期备份策略已生效。
    * 制定并演练“一键回滚”方案，确保在线上出现重大阻碍性 Bug 时，能快速恢复稳定。

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

# v0.0.7 需求分析与任务规划：用户管理 + 线上监控

## Context

v0.0.7 定位为"产品打磨与生产环境就绪"。当前项目无认证体系（`DEFAULT_USER_ID = "xxx"` 硬编码）、无 Rate Limiting、无 Token 消耗追踪。本计划覆盖两大模块：**用户管理系统建设** 和 **线上监控与可观测性建设**，产出详细的需求分析、任务分解、文件修改清单与自测流程。

---

## 模块 A：用户管理系统建设 (User Management System)

### A1. 需求分析

| 子功能 | 描述 | 优先级 |
|--------|------|--------|
| 注册/登录 | 邮箱+密码注册、登录、JWT 签发 | P0 |
| 密码找回 | 邮箱验证码重置密码 | P1 |
| JWT 认证中间件 | 全局路由鉴权，`/api/auth/*` 和 `/api/health` 白名单放行 | P0 |
| OAuth 扩展 | 预留 Google/GitHub OAuth 回调接口 | P2 |
| 用户角色 | `user` / `admin` 两级角色，Admin 可访问管理接口 | P0 |
| Admin 用户列表 | 查看注册用户、注册时间、状态 | P0 |
| Admin 封禁/解封 | 修改用户状态（active/banned） | P0 |
| Admin Token 额度配置 | 为用户设定 Token 使用上限 | P1 |
| 前端登录页 | 登录/注册表单 + Token 存储 + 自动跳转 | P0 |
| 前端 Admin 页面 | 用户列表 + 封禁/解封 + 额度设置 | P0 |

### A2. 数据结构设计

#### MySQL 表：`users`

```sql
CREATE TABLE IF NOT EXISTS users (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    email         VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname      VARCHAR(100) DEFAULT '',
    role          ENUM('user', 'admin') DEFAULT 'user',
    status        ENUM('active', 'banned') DEFAULT 'active',
    token_quota   BIGINT DEFAULT 1000000,         -- 默认 100 万 token 额度
    token_used    BIGINT DEFAULT 0,               -- 已消耗 token
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_email (email),
    INDEX idx_status (status)
);
```

#### MySQL 表：`password_reset_tokens`

```sql
CREATE TABLE IF NOT EXISTS password_reset_tokens (
    id         INT AUTO_INCREMENT PRIMARY KEY,
    user_id    INT NOT NULL,
    token      VARCHAR(255) NOT NULL UNIQUE,
    expires_at DATETIME NOT NULL,
    used       TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

#### Redis 键：

- `auth:blocklist:{jti}` — JWT 黑名单（登出/强制失效），TTL = token 剩余有效期
- `user:quota:{user_id}` — 缓存用户额度信息，TTL = 5 min

### A3. API 设计

#### 认证接口 (无需 JWT)

| Method | Path | 描述 | Request | Response |
|--------|------|------|---------|----------|
| POST | `/api/auth/register` | 注册 | `{email, password, nickname?}` | `{user_id, token}` |
| POST | `/api/auth/login` | 登录 | `{email, password}` | `{user_id, token, role}` |
| POST | `/api/auth/refresh` | 刷新 Token | Header: `Authorization` | `{token}` |
| POST | `/api/auth/forgot-password` | 发送重置邮件 | `{email}` | `{message}` |
| POST | `/api/auth/reset-password` | 重置密码 | `{token, new_password}` | `{message}` |

#### 用户接口 (需 JWT)

| Method | Path | 描述 |
|--------|------|------|
| GET | `/api/user/profile` | 获取当前用户信息 |
| PUT | `/api/user/profile` | 修改昵称等基本信息 |
| PUT | `/api/user/password` | 修改密码 |

#### Admin 接口 (需 JWT + role=admin)

| Method | Path | 描述 |
|--------|------|------|
| GET | `/api/admin/users` | 用户列表（分页+搜索） |
| PUT | `/api/admin/users/{user_id}/status` | 封禁/解封 |
| PUT | `/api/admin/users/{user_id}/quota` | 设置 Token 额度 |
| GET | `/api/admin/users/{user_id}/token-usage` | 查看用户 Token 消耗明细 |
| GET | `/api/admin/dashboard` | 全局概览（总用户/活跃/Token消耗） |

### A4. 任务分解与文件修改清单

#### Task A-1: 后端用户模型与数据库迁移（新增）
- **新增** `src/models/user.py` — User / PasswordResetToken Pydantic 模型
- **新增** `src/auth/` 目录：
  - `src/auth/__init__.py`
  - `src/auth/password.py` — bcrypt 哈希/验证（复用已有 `bcrypt==5.0.0` 依赖）
  - `src/auth/jwt_handler.py` — JWT 签发/验证/黑名单（使用 `PyJWT`，需新增依赖）
  - `src/auth/middleware.py` — FastAPI Depends 依赖注入，解析 Bearer Token → User
  - `src/auth/oauth.py` — OAuth 预留骨架（Google/GitHub provider 接口定义）
- **修改** `src/config.py` — 新增 `JWT_SECRET_KEY`, `JWT_ALGORITHM`, `JWT_EXPIRE_MINUTES`, `SMTP_*` 配置
- **修改** `src/frontend/context/storage/prod_storage.py` — 新增 `_ensure_users_table()` 初始化
- **修改** `src/frontend/context/storage/test_storage.py` — 新增 users 表 SQLite 版本

#### Task A-2: 后端认证 API（新增路由组）
- **修改** `src/api/app.py` — 新增 `/api/auth/*` 路由（register/login/refresh/forgot/reset）
- **修改** `src/api/app.py` — 全局挂载 JWT 认证中间件（白名单：`/api/auth/*`, `/api/health`, `/docs`）
- **修改** `src/api/app.py` — 所有已有路由从 request body 的 `user_id` 迁移到从 JWT token 提取

#### Task A-3: 后端用户管理 API
- **修改** `src/api/app.py` — 新增 `/api/user/profile` (GET/PUT)、`/api/user/password` (PUT)

#### Task A-4: 后端 Admin API
- **修改** `src/api/app.py` — 新增 `/api/admin/users`、`/api/admin/users/{id}/status`、`/api/admin/users/{id}/quota`、`/api/admin/dashboard`
- **修改** `src/auth/middleware.py` — Admin 角色校验依赖 `require_admin`

#### Task A-5: 前端认证流程
- **新增** `web/src/api/authApi.js` — 登录/注册/刷新 API 封装
- **修改** `web/src/api/httpClient.js` — 请求拦截器自动携带 `Authorization: Bearer <token>`，401 自动跳转登录
- **新增** `web/src/hooks/useAuth.js` — 登录态管理（token 存储/刷新/登出）
- **新增** `web/src/components/LoginPage.jsx` — 登录/注册表单（Ant Design Form）
- **修改** `web/src/App.jsx` — 增加路由守卫，未登录跳转 LoginPage
- **修改** `web/src/constants/appConfig.js` — 移除 `DEFAULT_USER_ID` 硬编码，新增 `AUTH_TOKEN_KEY`

#### Task A-6: 前端 Admin 页面
- **新增** `web/src/components/AdminPage.jsx` — 用户列表 Table + 封禁/解封/额度编辑
- **新增** `web/src/api/adminApi.js` — Admin API 封装
- **修改** `web/src/App.jsx` — 新增 Admin Tab（仅 admin 角色可见）

### A5. 验收用例

| # | 用例 | 预期结果 |
|---|------|----------|
| 1 | 新用户注册（有效邮箱+密码≥8位） | 返回 JWT Token，用户写入 DB |
| 2 | 重复邮箱注册 | 返回 409 错误 |
| 3 | 正确邮箱密码登录 | 返回 JWT Token + role |
| 4 | 错误密码登录 | 返回 401 |
| 5 | 被封禁用户登录 | 返回 403 "账号已被禁用" |
| 6 | 未携带 Token 访问 `/api/sessions/start` | 返回 401 |
| 7 | 携带有效 Token 访问业务接口 | 正常响应 |
| 8 | Token 过期后访问 | 返回 401 |
| 9 | 刷新 Token | 返回新 Token |
| 10 | 修改密码后旧 Token 失效 | 旧 Token 401 |
| 11 | Admin 查看用户列表 | 返回分页用户数据 |
| 12 | Admin 封禁用户 | 用户 status → banned |
| 13 | Admin 设置用户额度 | quota 更新，超额后请求被拒绝 |
| 14 | 普通用户访问 Admin 接口 | 返回 403 |
| 15 | 前端未登录自动跳转登录页 | 渲染 LoginPage |

---

## 模块 B：线上监控与可观测性建设 (Monitoring & Observability)

### B1. 需求分析

| 子功能 | 描述 | 优先级 |
|--------|------|--------|
| 请求时间线 / 审计时间线 | 记录用户登录、核心功能调用、异常行为 | P0 |
| Rate Limiting | 按用户/IP 频率限制，防刷量 | P0 |
| Token 消耗统计 | 每次 LLM 调用记录 prompt_tokens / completion_tokens / model / cost | P0 |
| 用户额度预警 | 单用户额度低于 20% 告警，耗尽熔断 | P1 |
| 平台总额预警 | 全局 Token 余额预警 → 管理员通知 | P1 |
| Health Check 增强 | 覆盖 Redis/MySQL/Chroma/LLM 的连通性检查 | P0 |
| 全局异常捕获 | 后端 unhandled exception → 结构化日志 + 告警 | P0 |
| 前端错误上报 | 前端 JS 异常 → `/api/monitor/error` | P1 |
| 基础指标面板 | Admin 可查看系统运行指标 | P1 |

### B2. 数据结构设计

#### MySQL 表：`token_usage_log`

```sql
CREATE TABLE IF NOT EXISTS token_usage_log (
    id               INT AUTO_INCREMENT PRIMARY KEY,
    user_id          INT NOT NULL,
    session_id       VARCHAR(255) DEFAULT '',
    request_path     VARCHAR(255) NOT NULL,          -- 调用接口路径
    model_name       VARCHAR(100) NOT NULL,           -- 使用的模型
    prompt_tokens    INT DEFAULT 0,
    completion_tokens INT DEFAULT 0,
    total_tokens     INT DEFAULT 0,
    estimated_cost   DECIMAL(10, 6) DEFAULT 0,        -- 估算费用 (USD)
    stage            VARCHAR(50) DEFAULT '',           -- analysis / generation / rag
    created_at       DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at),
    INDEX idx_user_time (user_id, created_at)
);
```

#### MySQL 表：`audit_log`

```sql
CREATE TABLE IF NOT EXISTS audit_log (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    user_id     INT DEFAULT NULL,
    action      VARCHAR(100) NOT NULL,               -- login / register / flow_stream / trip_update / admin_ban 等
    ip_address  VARCHAR(45) DEFAULT '',
    user_agent  VARCHAR(500) DEFAULT '',
    detail      JSON DEFAULT NULL,                    -- 可选扩展字段
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_action (user_id, action),
    INDEX idx_created_at (created_at)
);
```

#### Redis 键：

- `ratelimit:{user_id}:{endpoint}:{window}` — 滑动窗口计数器，TTL = window
- `token:global:used` — 全局 Token 消耗累计
- `alert:quota:{user_id}` — 用户额度预警标记，避免重复告警

### B3. API 设计

#### 监控接口 (需 JWT)

| Method | Path | 描述 |
|--------|------|------|
| GET | `/api/health` | 增强版健康检查（无需 JWT） |
| POST | `/api/monitor/error` | 前端错误上报 |
| GET | `/api/admin/token-usage/summary` | 全局 Token 消耗聚合（Admin） |
| GET | `/api/admin/token-usage/daily` | 按天 Token 消耗趋势（Admin） |
| GET | `/api/admin/audit-log` | 请求时间线查询（Admin） |
| GET | `/api/admin/system/health` | 系统详细健康状态（Admin） |

### B4. 任务分解与文件修改清单

#### Task B-1: Token 消耗追踪
- **新增** `src/observability/token_tracker.py` — `TokenTracker` 类，提供 `record_usage(user_id, session_id, path, model, prompt_tokens, completion_tokens, cost)` 方法写入 MySQL
- **修改** `src/llm/llm_manager.py` — 在 `analyze_user_message` / `generate_trip` / `change_trip` 等 LLM 调用点，提取 token usage 并调用 TokenTracker
- **修改** `src/llm/streaming_adapter.py` — stream 结束时回调 token 统计
- **修改** `src/config.py` — 新增 `PLATFORM_TOKEN_BUDGET`, `USER_DEFAULT_TOKEN_QUOTA`, `TOKEN_ALERT_THRESHOLD_PCT`

#### Task B-2: Rate Limiting 中间件
- **新增** `src/observability/rate_limiter.py` — 基于 Redis 的滑动窗口限流器
- **修改** `src/api/app.py` — 全局 middleware 或 Depends 注入，按 `user_id + endpoint` 维度限流
- **修改** `src/config.py` — 新增 `RATE_LIMIT_*` 配置（默认: 60次/分钟/用户, 10次/分钟/flow_stream）

#### Task B-3: 请求时间线 / 审计时间线
- **新增** `src/observability/audit.py` — `AuditLogger` 类，`log(user_id, action, ip, user_agent, detail)` 写入 MySQL，请求时间线/审计时间线统一落库
- **修改** `src/api/app.py` — 在关键操作（login/register/flow_stream/trip_update/admin操作）埋点

#### Task B-4: 用户额度熔断
- **修改** `src/observability/token_tracker.py` — 新增 `check_quota(user_id)` 方法，查询 users 表 quota vs used
- **修改** `src/auth/middleware.py` — 在认证中间件中增加额度校验，额度耗尽返回 429
- **修改** `src/observability/errors.py` — 新增 `QUOTA_EXCEEDED`, `RATE_LIMITED` 错误码

#### Task B-5: Health Check 增强
- **修改** `src/api/app.py` — `/api/health` 增加 Redis/MySQL/Chroma 连通性检测
- **新增** `src/observability/health.py` — 各组件 health check 函数

#### Task B-6: 全局异常处理
- **修改** `src/api/app.py` — FastAPI `@app.exception_handler` 全局异常捕获 → 结构化日志 + 审计
- **修改** `src/observability/errors.py` — 补充 HTTP 状态码映射

#### Task B-7: 前端错误上报与监控面板
- **新增** `web/src/api/monitorApi.js` — 错误上报 API
- **修改** `web/src/App.jsx` — `window.onerror` / `unhandledrejection` 全局捕获
- **修改** `web/src/components/AdminPage.jsx` — 增加监控 Tab（Token 消耗趋势图、请求时间线表格、系统健康状态）

### B5. 验收用例

| # | 用例 | 预期结果 |
|---|------|----------|
| 1 | 发起 `/api/flow/stream` 后查询 token_usage_log | 新增一条记录，包含 model/tokens/cost |
| 2 | 同一用户 1 分钟内连续调用 61 次普通接口 | 第 61 次返回 429 |
| 3 | 1 分钟内调用 11 次 `/api/flow/stream` | 第 11 次返回 429 |
| 4 | 用户 token_used 达到 token_quota | 后续 LLM 请求返回 429 "额度已用完" |
| 5 | Admin 查看全局 Token 消耗 | 返回按天聚合的消耗数据 |
| 6 | Admin 查看请求时间线 | 包含 login/flow_stream 等操作记录 |
| 7 | Redis 断开后调用 `/api/health` | 返回 `{status: "degraded", redis: "down"}` |
| 8 | 前端 JS 异常触发 | 自动 POST 到 `/api/monitor/error` |
| 9 | 后端 unhandled exception | 返回 500 + 结构化错误 JSON，请求时间线有记录 |
| 10 | 额度低于 20% 时 Admin 面板显示预警 | 用户行显示黄色警告标识 |

---

## 模块 C：线上部署（Docker Compose）

### C1. 任务分解与文件清单

#### Task C-1: Docker 化
- **新增** `Dockerfile` — Python 后端镜像（基于 python:3.11-slim）
- **新增** `web/Dockerfile` — React 前端镜像（基于 node:20-alpine + nginx）
- **新增** `web/nginx.conf` — Nginx 配置（静态资源 + API 反向代理）
- **新增** `docker-compose.yml` — 编排 frontend / backend / redis / mysql 四个服务
- **新增** `.env.production` — 生产环境变量模板
- **新增** `sql/auth/init_mysql.sql`、`sql/auth/init_sqlite.sql`、`sql/auth/queries.sql` — 认证相关建表与运行时 SQL

#### Task C-2: CI/CD 基础
- **新增** `.github/workflows/deploy.yml` — GitHub Actions：lint → test → build → push image
- **新增** `scripts/rollback.sh` — 一键回滚脚本（docker-compose down + 切换版本 + up）

### C2. 验收用例

| # | 用例 | 预期结果 |
|---|------|----------|
| 1 | `docker-compose up -d` | 所有服务正常启动 |
| 2 | 浏览器访问前端 | 显示登录页 |
| 3 | 登录后正常使用行程功能 | SSE 流式正常 |
| 4 | `docker-compose down && docker-compose up -d` | 数据持久化不丢失 |

---

## 开发顺序建议

```
Phase 1 (P0 基础): A-1 → A-2 → A-5 → B-5 → B-6
  - 用户模型 + JWT 认证 + 前端登录 + Health Check + 异常处理
  - 产出：可登录的系统，基础安全防护

Phase 2 (P0 管理): A-3 → A-4 → A-6 → B-2 → B-3
  - 用户管理 + Admin 页面 + Rate Limiting + 请求时间线
  - 产出：管理闭环

Phase 3 (P1 监控): B-1 → B-4 → B-7
  - Token 追踪 + 额度熔断 + 前端监控面板
  - 产出：可观测、可控制的 Token 消耗体系

Phase 4 (部署): C-1 → C-2
  - Docker 化 + CI/CD
  - 产出：一键部署能力
```

---

## 新增/修改文件汇总

### 新增文件（17 个）
| 文件 | 用途 |
|------|------|
| `src/models/user.py` | 用户 Pydantic 模型 |
| `src/auth/__init__.py` | auth 模块 |
| `src/auth/password.py` | 密码哈希 |
| `src/auth/jwt_handler.py` | JWT 签发/验证 |
| `src/auth/middleware.py` | 认证中间件 |
| `src/auth/oauth.py` | OAuth 预留骨架 |
| `src/observability/token_tracker.py` | Token 消耗追踪 |
| `src/observability/rate_limiter.py` | Rate Limiting |
| `src/observability/audit.py` | 请求时间线 / 审计时间线 |
| `src/observability/health.py` | 健康检查 |
| `web/src/api/authApi.js` | 认证 API |
| `web/src/api/adminApi.js` | Admin API |
| `web/src/api/monitorApi.js` | 监控 API |
| `web/src/hooks/useAuth.js` | 登录态 Hook |
| `web/src/components/LoginPage.jsx` | 登录页 |
| `web/src/components/AdminPage.jsx` | Admin 页 |
| `sql/auth/init_mysql.sql` | MySQL 认证表初始化 |
| `sql/auth/init_sqlite.sql` | SQLite 认证表初始化 |
| `sql/auth/queries.sql` | 认证 / 审计 / 限流运行时 SQL |

### 修改文件（10 个）
| 文件 | 修改内容 |
|------|----------|
| `src/config.py` | 新增 JWT / SMTP / RateLimit / TokenBudget 配置 |
| `src/api/app.py` | 新增路由组 + 中间件挂载 + 审计埋点 + Health 增强 |
| `src/llm/llm_manager.py` | LLM 调用后 token 用量上报 |
| `src/llm/streaming_adapter.py` | 流式结束后 token 统计 |
| `src/observability/errors.py` | 新增错误码 |
| `src/frontend/context/storage/prod_storage.py` | 新增 users / token_usage / audit 建表 |
| `src/frontend/context/storage/test_storage.py` | 对应 SQLite 版本 |
| `web/src/api/httpClient.js` | 携带 Bearer Token + 401 处理 |
| `web/src/App.jsx` | 路由守卫 + Admin Tab + 错误捕获 |
| `web/src/constants/appConfig.js` | 移除硬编码 user_id |

### 部署相关新增（5 个）
| 文件 | 用途 |
|------|------|
| `Dockerfile` | 后端镜像 |
| `web/Dockerfile` | 前端镜像 |
| `web/nginx.conf` | Nginx 配置 |
| `docker-compose.yml` | 服务编排 |
| `.env.production` | 生产环境变量 |

### 新增依赖
- 后端：`PyJWT>=2.8.0`
- 前端：`react-router-dom`（路由守卫）

---

## 当前建议自测流程

### 1. 后端认证自测
```bash
# 启动后端
source venv/bin/activate && PYTHONPATH=. uvicorn src.api.app:app --host 127.0.0.1 --port 8000

# 注册
curl -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test1234"}'

# 登录
curl -X POST http://127.0.0.1:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test1234"}'
# → 记录返回的 token

# 携带 Token 访问业务接口
curl -X POST http://127.0.0.1:8000/api/sessions/start \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"device_id":"web"}'

# 不携带 Token → 401
curl http://127.0.0.1:8000/api/sessions/list

# Health Check
curl http://127.0.0.1:8000/api/health
```

### 2. Rate Limiting 自测
```bash
# 快速连续调用测试（超过阈值触发 429）
for i in $(seq 1 65); do
  curl -s -o /dev/null -w "%{http_code}\n" \
    -H "Authorization: Bearer <token>" \
    http://127.0.0.1:8000/api/sessions/list
done
```

### 3. Token 消耗追踪自测
```bash
# 发起一次行程生成
curl -X POST http://127.0.0.1:8000/api/flow/stream \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"session_id":"xxx","message":"帮我规划东京三日游"}'

# 查询 Token 消耗
curl -H "Authorization: Bearer <admin_token>" \
  "http://127.0.0.1:8000/api/admin/token-usage/summary"
```

### 4. Admin 功能自测
```bash
# Admin 用户列表
curl -H "Authorization: Bearer <admin_token>" \
  "http://127.0.0.1:8000/api/admin/users"

# 封禁用户
curl -X PUT http://127.0.0.1:8000/api/admin/users/2/status \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"status":"banned"}'

# 设置用户额度
curl -X PUT http://127.0.0.1:8000/api/admin/users/2/quota \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{"token_quota":500000}'
```

### 5. 前端自测
```
1. 打开 http://localhost:5173 → 应跳转到登录页
2. 注册新用户 → 自动登录 → 进入主页
3. 刷新页面 → 保持登录态（localStorage token）
4. Admin 用户看到 "管理" Tab → 普通用户不可见
5. Admin 页面：用户列表加载 / 封禁 / 额度设置正常
6. 登出 → 跳转登录页
```

### 6. Docker 部署自测
```bash
docker-compose up -d
# 访问 http://localhost → 前端页面
# 访问 http://localhost/api/health → 健康检查
docker-compose down
docker-compose up -d  # 验证数据持久化
```

### 7. 自测用例文件
- **新增** `handlers/auth_test.py` — 注册/登录/JWT/角色校验自动化测试
- **新增** `handlers/monitoring_test.py` — Rate Limiting / Token 追踪 / 请求时间线测试




-------------------


# 实现逻辑总结

## 1. 模块 A：用户管理系统建设 (User Management System)

### 1.1 任务规划（前端 / 后端 / API / 数据结构 / 验收用例）

- 当前已直接进入 Phase 1 主链路改造，不再保留 `DEFAULT_USER_ID` 等旧兼容方案。
- 本阶段后端聚焦：
  - 新增 `src/auth/` 认证模块，拆分密码哈希、JWT 签发校验、认证依赖、OAuth 预留骨架。
  - 新增 `src/models/user.py`，统一用户公开信息与重置 token 数据结构。
  - 在 `src/api/app.py` 内新增认证/用户接口，并把会话、聊天、主流程、知识库、行程编辑接口统一切到 JWT 鉴权。
- 本阶段前端聚焦：
  - 新增 `web/src/api/authApi.js`、`web/src/hooks/useAuth.js`、`web/src/components/LoginPage.jsx`。
  - `web/src/api/httpClient.js` 统一注入 `Authorization: Bearer <token>`，401 直接清登录态并回到登录页。
  - `web/src/App.jsx` 改为“未登录显示登录页，登录后进入业务主界面”的单入口结构。
- 本阶段暂不实现：
  - OAuth 真接入
  - 密码找回邮件发送
  - 额度熔断 / Rate Limiting / 请求时间线
- 当前验收目标收敛为：
  - 注册成功后自动获得 token 并进入主页面
  - 登录后所有业务请求自动带 JWT
  - 未登录或 token 失效时，现有业务接口统一拒绝访问
  - 原有会话 / 主流程 / 行程编辑链路全部由当前登录用户驱动

### 1.2 当前实际开发逻辑（截至当前代码）

- 用户数据存储：
  - 当前未沿用旧会话存储表去塞用户字段，而是新增独立 `auth.db` SQLite 库。
  - `users` 表包含 `email/password_hash/nickname/role/status/token_quota/token_used/token_version/created_at/updated_at`。
  - `password_reset_tokens`、`auth_blocklist`、`token_usage_log`、`audit_log`、`rate_limit_log` 已统一建表。
- JWT 认证逻辑：
  - `src/auth/jwt_handler.py` 采用 HS256 生成/校验 token，payload 包含 `sub/email/role/token_version/iat/nbf/exp/jti`。
  - `src/auth/password.py` 使用 `bcrypt` 做密码哈希和校验。
  - `src/auth/middleware.py` 提供 `get_current_user/get_optional_user/require_admin`，统一从 `Authorization` 解析登录用户。
  - 后端在鉴权时会校验用户状态和 `token_version`，因此修改密码后旧 token 会直接失效。
- 管理员与用户管理逻辑：
  - 当前采用“首个注册用户自动成为 admin，后续注册用户默认 user”的策略。
  - 已新增管理员接口：
    - `GET /api/admin/users`
    - `PUT /api/admin/users/{user_id}/status`
    - `PUT /api/admin/users/{user_id}/quota`
    - `GET /api/admin/dashboard`
    - `GET /api/admin/users/{user_id}/token-usage`
    - `GET /api/admin/audit-logs`
  - 管理员可以分页查用户、按邮箱/昵称搜索、封禁/解封、调整额度，并查看 token 使用日志与请求时间线。
  - Dashboard 当前展示总用户数、活跃数、封禁数、管理员数、总配额、总已用、剩余额度。
- 后端接口改造：
  - 已新增：
    - `POST /api/auth/register`
    - `POST /api/auth/login`
    - `POST /api/auth/refresh`
    - `POST /api/auth/forgot-password`
    - `POST /api/auth/reset-password`
    - `GET /api/user/profile`
    - `PUT /api/user/profile`
    - `PUT /api/user/password`
  - 已切换到 JWT 鉴权：
    - `/api/sessions/*`
    - `/api/chat/send`
    - `/api/flow/*`
    - `/api/map/*`
    - `/api/trip/*`
    - `/api/knowledge/*`
  - `GET /api/health` 保留可匿名访问，但会额外返回当前是否已携带有效登录态。
  - 会话接口已加入归属校验，不能跨用户读取、删除、续用别人的 `session_id`。
- 限流与安全控制：
  - 认证接口（注册 / 登录 / 找回 / 重置）当前按 IP 做窗口限流。
  - 已登录业务接口当前按 `user_id` 做窗口限流，管理员走更高阈值。
  - 限流命中后会返回 429，同时写入 `audit_log`，便于排查恶意请求或用户误触发。
- Token 消耗自动累计：
  - `src/llm/llm_manager.py` 已新增 usage observer，能够在真实 LLM 调用完成后把 `prompt_tokens/completion_tokens/total_tokens/model_name/stage` 回传给 API 层。
  - 聊天、主流程中的意图识别、工具路由、FunctionCall、流式生成、非流式生成，都会通过 observer 自动落到 `token_usage_log`。
  - `knowledge/search` 与 `knowledge/answer_from_evidence` 的回答生成链路当前通过统一 token 估算口径补记到 `token_usage_log`，并同步累计到 `users.token_used`。
  - 当前 token 统计仍属于“统一估算口径”，不是底层模型服务商返回的精确 billing token，但已经能形成单用户累计消耗闭环。
- 请求时间线 / 审计时间线：
  - 注册、登录、找回密码、重置密码、额度阻断、限流命中、主流程启动/完成、聊天发送、管理员改状态、管理员改额度等关键动作都会写入 `audit_log`。
  - `/api/sessions/*`、`/api/trip/*`、知识库管理相关接口（知识库列表/创建/删除/上传/链接预处理/链接导入/来源列表/来源删除/来源更新）也已经统一写入审计，管理后台可以看到更完整的用户操作轨迹。
  - 请求时间线字段已扩展为 `user_id/user_email/action/session_id/message_id/request_path/status/detail_json/ip_address/created_at`，足够支撑后续问题追溯与会话时间线查询。
- 额度门禁：
  - 后端已在核心高成本接口增加 `_assert_within_quota`：
    - `/api/chat/send`
    - `/api/flow/stream`
    - `/api/trip/update`
    - `/api/trip/replan_day`
    - `/api/knowledge/search`
    - `/api/knowledge/answer_from_evidence`
  - 当用户 `token_used >= token_quota` 时，接口直接返回 `429 Token 额度已用完，请联系管理员`，并同步写入请求时间线。
- 前端登录流：
  - `web/src/hooks/useAuth.js` 在启动时检查本地 token，有 token 就拉取 `/api/user/profile` 恢复登录态。
  - `web/src/components/LoginPage.jsx` 提供登录 / 注册双模式表单，注册成功后直接进入系统。
  - `web/src/App.jsx` 不再依赖硬编码用户，页面根节点先判断 `isAuthenticated`，未登录只渲染登录页。
  - 顶部 Header 已接入当前用户信息和“退出登录”动作。
  - 前端通过 `authUser.role` 判断是否展示“管理”Tab。
- 前端请求层改造：
  - `web/src/api/httpClient.js` 已统一封装 token 读写、请求头注入和 401 事件广播。
  - 请求层已补充 429/403 友好错误文案：额度耗尽会提示联系管理员，限流命中会提示稍后重试，封禁账号会提示联系管理员。
  - `web/src/api/flowApi.js` 的 SSE 请求已补齐 Bearer Token。
  - `web/src/api/sessionApi.js` 已移除 `user_id` 查询参数写法，直接走当前登录用户。
  - `web/src/hooks/useSessions.js`、`web/src/hooks/useTrip.js` 已移除 `DEFAULT_USER_ID`，只保留 `device_id + session_id`。
  - `web/src/components/AdminPage.jsx` 已支持查看概览统计、搜索用户、封禁/解封、在线编辑并保存额度。
  - 管理页已新增“请求时间线”视图，可按 `action/user_id/session_id/message_id/request_path` 筛选查看 `audit_log`；用户表也已支持打开单用户 `token_usage_log` 抽屉查看最近消耗明细。
- 当前未完成项：
  - 真正的 SMTP 发信与 OAuth 登录还只是预留接口，不是生产完成态。
  - `forgot-password` 目前是开发态返回 reset token，不走邮件通道。
  - Rate Limiting 目前是单机 SQLite + 时间窗口实现，适合作为当前单体项目的基础防护，后续若改成多实例部署需要迁移到 Redis 等集中式存储。
  - token 统计目前采用统一估算口径，后续如需成本对账，仍建议补接模型服务商返回的精确 usage 字段。
  - 本轮代码已经为认证主链、Admin 管理页、请求鉴权、额度门禁补充了详细注释，后续维护时可以直接从源码注释理解设计原因和边界。

### 1.3 自测流程

1. 后端启动后，先访问 `GET /api/health`，确认返回 `status=ok`。
2. 前端打开 `http://localhost:5173`，应先进入登录页，而不是直接进入主工作台。
3. 在登录页注册新账号：
   - 输入有效邮箱
   - 输入不少于 8 位的密码
   - 注册成功后应自动进入主页面
4. 注册后刷新浏览器：
   - 应继续保持登录态
   - 会话列表和知识库页面可正常加载
5. 在主页面执行一次核心链路：
   - 新建会话
   - 发送聊天消息
   - 触发行程生成
   - 进入旅行灵感页做一次知识库请求
   - 以上请求都不再需要传 `DEFAULT_USER_ID`
6. 点击右上角“退出登录”：
   - 本地 token 清空
   - 页面应立即回到登录页
7. 手动清空或伪造 token 后再次刷新：
   - 前端应回到登录页
   - 后端业务接口应返回 401/403，而不是继续放行
8. 修改密码后，用旧 token 重新请求接口：
   - 旧 token 应失效
   - 需要重新登录获取新 token
9. 使用首个注册账号登录：
   - 应看到“管理”Tab
   - 进入后应能加载 Dashboard 和用户列表
10. 使用第二个普通账号登录：
   - 不应看到“管理”Tab
   - 直接访问 Admin 接口应返回 403
11. 使用管理员封禁普通用户：
   - 普通用户再次登录应失败，返回“账号已被禁用”
12. 使用管理员把普通用户额度设为 0：
   - 该用户调用主流程 / 对话 / 知识检索时应返回 429
13. 管理员恢复用户状态并提高额度后：
   - 该用户应重新恢复业务访问能力
14. 连续高频访问登录接口或业务接口：
   - 应命中 429 限流
   - 后端 `audit_log` 中应能查到 `rate_limit_blocked`
15. 触发聊天 / 主流程 / 知识回答后：
   - `token_usage_log` 应出现对应记录
   - `users.token_used` 应自动累加
16. 管理员访问 token usage / audit logs 接口：
   - 应能看到单用户 token 消耗记录与关键审计动作
17. 使用普通用户完整执行一次“会话创建 -> 聊天 -> 行程编辑/局部重排 -> 知识库操作”链路后：
   - Admin 页“请求时间线”中应能按 `user_id` 或 `session_id` 筛到这条链路的连续请求记录
   - 涉及主流程流式请求时，日志中还应带出对应 `message_id`


## 2. 模块 B：线上监控与可观测性建设 (Monitoring & Observability)

### 2.1 当前实际开发逻辑（截至当前代码）

- 监控数据存储：
  - 当前监控相关数据统一先落在 `auth.db`，包括 `token_usage_log`、`audit_log`、`rate_limit_log`。
  - 主流程执行指标仍然保留在原有 `flow_metrics.db`，两套数据暂时分库存放。
- Token 消耗追踪：
  - `LlmManager` 通过 usage observer 将真实 LLM 调用摘要上报给 API 层。
  - API 层收到回调后会同时写 `token_usage_log` 并累计 `users.token_used`。
  - `knowledge/search` 与 `knowledge/answer_from_evidence` 的回答生成也已补做 token 估算落库。
- 请求时间线 / 审计时间线：
  - 当前已覆盖认证行为、主流程启动/完成、聊天发送、限流命中、额度阻断、管理员操作，以及 `sessions/trip/knowledge` 相关管理动作。
  - 请求时间线已经补充 `session_id/message_id`，可以支持按用户、按会话、按消息做请求时间线查询。
  - 请求时间线已经可以通过管理员接口查询，并已接到前端 Admin 页的“请求时间线”视图做筛选展示。
- Rate Limiting：
  - 当前实现采用 SQLite 时间窗口方案：
    - 匿名认证接口按 IP 限流
    - 已登录业务接口按 `user_id` 限流
    - 管理员走更高阈值
  - 这是单机部署下的基础版实现，后续若进入多实例部署，需要迁移到 Redis。
- 前端监控可视化：
  - Admin 页当前已经可以直接查看 Dashboard、请求时间线列表、单用户 token 使用日志抽屉，满足基础运营排查场景。
  - 前端请求层已经把额度命中、限流命中、账号封禁转换成更友好的提示文案，减少普通用户看到原始后端报错。
- 当前未完成项：
  - 尚未接入平台总额预警、单用户额度低于阈值告警。
  - 尚未接入前端异常上报和系统资源监控。

### 2.2 自测流程

1. 启动后端与前端后，先使用管理员账号登录，进入“管理”Tab，确认能看到概览卡片、用户管理、请求时间线三个基础区域。
2. 在普通用户侧依次执行注册、登录、聊天发送、主流程生成、知识检索各一次：
   - Admin 页“请求时间线”中应出现 `auth_register`、`auth_login`、`chat_send`、`flow_stream_started`、`flow_stream_completed` 等记录。
3. 在普通用户侧继续执行一次 `session_start -> session_history/session_trip -> trip_update 或 trip_replan_day -> knowledge_*` 操作链：
   - Admin 页“请求时间线”应能看到对应的会话/行程/知识库请求记录。
4. 在 Admin 页“请求时间线”中切换动作筛选，或输入 `user_id/session_id/message_id/request_path`：
   - 列表应只显示匹配条件的请求记录。
   - 其中主流程相关记录应能看到 `message_id`，会话相关记录应能看到 `session_id`。
5. 在普通用户侧触发一次聊天或主流程后，回到 Admin 页用户列表，点击某个用户的“Token 日志”：
   - 右侧抽屉应打开，并展示该用户最近的 `model_name/stage/request_path/prompt_tokens/completion_tokens/total_tokens`。
6. 对同一个普通用户多执行几次聊天、主流程、知识回答：
   - “Token 日志”抽屉中的记录条数应增加。
   - 用户列表中的 `token_used / token_quota` 展示值应同步增长。
7. 将某个普通用户额度调整为较小值，随后继续发起聊天或主流程：
   - 前端应提示“当前账号的 AI 调用额度已用完，请联系管理员调整额度。”
   - Admin 页请求时间线中应出现 `quota_blocked`。
8. 连续高频点击登录或连续快速发送业务请求，触发限流：
   - 前端应提示“你的操作有点频繁了，请稍等片刻再试。”
   - Admin 页请求时间线中应出现 `rate_limit_blocked`。
9. 将某个用户封禁后再尝试登录：
   - 前端应提示“当前账号已被禁用，请联系管理员。”
   - 请求时间线中应能看到对应失败记录。
10. 刷新 Admin 页：
   - Dashboard、用户列表、请求时间线筛选结果、Token 日志查看能力应继续可用，不需要重新手动拼接任何调试参数。
11. 说明：
   - 当前 `token_usage_log` 使用的是统一估算口径，适合运营排查和额度控制；
   - 若后续需要做财务级成本对账，还需要补接模型服务商返回的精确 usage/billing 字段。

## 3. 模块 C：线上部署架构重构与增强版实现 (Production Deployment & Tier-B Enhancement)

### 3.1 任务规划与实际开发逻辑（针对 Render / Vercel / TiDB Serverless）

为了保证首期低成本、免运维上线（即线上部署方案“档位 B 增强版”），本版本在部署架构上进行了针对性重构，脱离了单纯的本地 Docker Compose 限制。
当前实际状态已经从“具备增强版主体能力”推进到“知识库导入链路关键阻断已修复，可用于低频演示”，但仍需继续保守看待本地持久化能力：

- **认证存储层重构 (TiDB / MySQL 支持)**：
  - 新增统一数据库访问层 `src/auth/database.py`，封装 `AuthDBConnection` 和 `AuthDBCursor`。
  - 自动根据环境变量 `AUTH_DB_BACKEND=mysql` 切换到底层 `mysql.connector` 驱动。
  - 智能转换 SQL 语法（如将 SQLite 的 `?` 转换为 MySQL 的 `%s`，`INSERT OR REPLACE` 转换为 `REPLACE`），实现业务层零感知迁移。
  - 在 `init_auth_tables` 中统一从 `sql/auth/init_mysql.sql` / `sql/auth/init_sqlite.sql` 读取建表语句，保证生产库能全新初始化。
  - `ProdConversationStorage` 已新增生产业务表自动建表逻辑，覆盖 `session_list/session_chat/core_entities/long_term_summaries/trip_data_store`。
  - 上述 5 张表的 DDL 已提取到 `sql/storage/prod/init_tables.sql`，后续改表结构只需要维护这一份 SQL 文件。
  - `src/api/app.py` 启动阶段会显式触发一次 `_get_storage()`，因此 Render 首次启动时就会自动完成业务表初始化，而不是等到第一条业务请求才发现缺表。

- **核心基础配置补齐 (Redis / TLS / CORS)**：
  - `src/config.py` 新增 `REDIS_URL` 和 `MYSQL_SSL_CA`。
  - `prod_storage.py` 支持通过 `REDIS_URL` 连接 Upstash Redis，并对 MySQL 注入 TLS 证书（TiDB Serverless 必备）。
  - `src/api/app.py` 中移除通配符 CORS，改为通过 `CORS_ORIGINS` 动态解析跨域白名单，满足 Vercel 前端域名安全调用。

- **生产环境 LLM 策略调整**：
  - `dependencies.py` 中当前按“用户私有配置优先、全局环境变量兜底”的方式实时构造 `LlmManager`。
  - `_get_llm_manager_for_user` 已不再使用 `lru_cache`，因此用户在前端保存新的私有 LLM 配置后可以立刻生效，不会出现缓存未刷新导致配置不生效的问题。
  - 这也意味着生产环境在用户未配置私有模型时，会自然回退到全局 `ANALYSIS_*` / `GENERATION_*` 环境变量，实现“部署后即开即用”。

- **恢复知识库增强 (移除重型本地模型接入云端)**：
  - **向量存储云端化**：`src/rag/store/vector_store.py` 移除本地 `SentenceTransformer`，改为通过 `langchain_openai.OpenAIEmbeddings` 调用云端 API（如 SiliconFlow 等带免费额度的服务）。
  - **意图识别轻量化**：`src/rag/module/intent_recognition.py` 彻底移除本地 Sentence-BERT，改用“规则（关键词探测）+ LLM”进行轻量级意图分类。
  - **意图识别容错补齐**：`intent_recognition.py` 中对 LLM 返回 JSON 的解析已经补了 `try/except`，即使模型返回非法 JSON，也会回退到兜底分类而不是直接中断链路。
  - **当前仍保留的低优先级问题**：`_looks_like_no_search_needed()` 仍是简单关键词匹配，存在误判空间，但对首期线上可用性影响不大，后续可再升级成更稳的规则或正则。
  - **知识库导入链路修复**：`src/api/routes/knowledge.py` 已补齐 `uuid4` / `_build_text_preview` 导入，上传、URL 导入、来源更新三条链路统一按 `add_documents()` 返回的整数 chunk 数量处理，避免运行时对 `int` 再做 `len()` 导致 500。
  - **知识库删除链路收口**：删除知识库时统一走 `VectorStore.delete_collection()`，避免路由层直接依赖底层向量实现细节。

- **依赖精简 (冷启动优化)**：
  - 从 `requirements.txt` 中大刀阔斧裁剪了 `torch`、`sentence-transformers`、`transformers`、`onnxruntime`、`safetensors` 等重型依赖。
  - `src/rag/network/crawler.py` 仍保留了可选的 Playwright L3 兜底代码，但默认部署链路不再依赖该运行时；若线上未安装 Playwright，会自动退化为 L1/L2 抓取。
  - 大幅缩小 Render 服务端构建镜像体积与内存占用，极大缓解免费实例休眠后的冷启动时长。

- **本轮代码审查后已经落实的稳定性修正**：
  - `is_token_blocked()` 不再每次鉴权都清理过期 token，而是改为低概率触发清理，减少 TiDB Serverless 下的额外写入压力。
  - `AuthDBCursor.fetchone()` / `fetchall()` 的 MySQL / SQLite 分支语义已经补充清晰注释，当前实现和业务预期一致。
  - `AuthDBConnection` 已支持 `with get_auth_db_connection() as conn:` 上下文管理，`dependencies.py`、`middleware.py` 等调用点也已统一收口，降低连接泄露风险。
  - `prod_storage.py` 与 `auth/database.py` 当前都统一使用 `if config.MYSQL_SSL_CA:` 注入 TLS 参数，不再混用不同写法。
  - `config.py` 已显式补充 `TRIP_CONTEXT_STORAGE_TYPE`，部署时不需要再依赖隐式默认推断。
  - 运行时执行 SQL 已统一迁移到 `sql/` 目录，通过 `src/utils/sql_loader.py` 加载，不再在 Python 代码中内嵌执行型 SQL 语句。

- **当前仍保留的工程性限制**：
  - MySQL 目前仍然是“短连接 + 每次请求新建连接”模式，正确性没有问题，但还没有进一步升级到连接池。
  - 因此当前架构更偏向“单体服务先稳定上线”，后续若流量提升，再继续做连接池和更细粒度的性能优化。

- **当前仍需保守说明的限制**：
  - 认证数据与会话持久化已经切到 TiDB / Redis，但知识库元数据与向量索引仍默认依赖本地 `chroma_db`。
  - 主流程指标仍默认写入本地 `flow_metrics.db`。
  - 因此“增强版”当前更适合低频演示与验证链路，不适合对外承诺稳定的长期持久化知识库。

### 3.2 自测与部署流程验证

1. **环境准备**：
   - 在 TiDB 申请 Serverless MySQL，并先创建 `chat_context` 数据库。
   - 在 Upstash 申请 Redis 并获取 `REDIS_URL`。
   - 在 Render 创建 Web Service，配置所有环境变量（包括 `TRIP_CONTEXT_STORAGE_TYPE=prod`、`AUTH_DB_BACKEND=mysql` 等）。
2. **部署与连通性验证**：
   - Render 部署成功后，访问 `https://<render-url>/api/health` 检查各服务（Redis/MySQL）连通性。
   - 首次启动日志中应能通过存储初始化阶段自动完成认证表与业务表建表；若云数据库权限受限，才回退执行 `sql/auth/init_mysql.sql` 与 `sql/storage/prod/init_tables.sql` 做手工兜底。
   - 验证 Vercel 部署的前端页面能否正常打开，并无 CORS 报错。
3. **数据链路与主流程验证**：
   - 注册新用户，验证数据是否成功写入 TiDB 的 `users` 表。
   - 执行对话与行程生成，检查 Render 单 worker 模式下 SSE 流式响应是否正常输出不断流。
4. **增强版（知识库）验证**：
   - 验证配置的云端 Embedding API 是否可用。
   - 尝试上传/预处理一份私有知识，验证上传、URL 导入、来源编辑三条链路都能成功落库并返回 chunk 数。
   - 验证检索结果能返回对应的命中来源。
   - 若 Render 实例重启后本地索引丢失，应按“重新导入知识源”的预期验收，而不是按永久持久化验收。
